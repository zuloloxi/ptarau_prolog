:-[moo].
:-[dual]. % will spawn a new server in a window
%:-[spanish].
:-[english].

password(none).

%home('http://localhost/'). % put home page ref. here

% change if in "multi" or "dual" mode and you want to connect to 
% a remote server

%host('139.103.16.2').
%port(7001).

go:-logimoo_client.
